import { knowledgeDataClientFlamingo } from './knowledge-data-client-flamingo';

describe('knowledgeDataClientFlamingo', () => {
  it('should work', () => {
    expect(knowledgeDataClientFlamingo()).toEqual(
      'knowledge-data-client-flamingo'
    );
  });
});
