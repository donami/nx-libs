import { widgetRuntimeFlamingo } from './widget-runtime-flamingo';

describe('widgetRuntimeFlamingo', () => {
  it('should work', () => {
    expect(widgetRuntimeFlamingo()).toEqual('widget-runtime-flamingo');
  });
});
